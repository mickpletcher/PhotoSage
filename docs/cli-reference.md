# CLI Reference

> Generated by `python scripts/generate_cli_reference.py`. Do not edit command tables manually.

This reference is generated from the Typer command tree, so command names, options, defaults, and help text match the executable.

## Safety conventions

- Commands that change filenames require an explicit apply flag.
- Prefer `preview`, then `review`, then `rename --manifest ... --apply`.
- `--local-only` blocks Anthropic, Gemini, Kimi, and OpenAI.
- `--output-json` files may contain local paths and must be treated as private.
- Use `--config` when the working directory does not contain `config/settings.yaml`.

## Top-level commands

| Command | Purpose |
| --- | --- |
| `photosage doctor` | Check configuration, providers, paths, and optional media tools. |
| `photosage recover` | Inspect, resume, or roll back an interrupted manifest. |
| `photosage review` | Review, approve, reject, or edit proposed manifest filenames. |
| `photosage providers` | Show configured provider availability and local/cloud status. |
| `photosage duplicates` | Find likely duplicate photos and optionally export duplicate groups as JSON. |
| `photosage organize-preview` | Compare folder organization policies without creating folders or moving files. |
| `photosage browse` | Generate private offline timeline and GPS map views. |
| `photosage watch` | Process stable files from a watched folder into an approval queue or apply after explicit approval. |
| `photosage astro` | Preview or apply astrophotography filenames with capture-night grouping and FITS metadata. |
| `photosage scan` | Scan supported image files, score metadata, and show whether AI would be required. |
| `photosage preview` | Preview proposed filename changes without renaming files. |
| `photosage rename` | Apply safe renames only when --apply is explicitly provided. |
| `photosage lightroom-process` | Process Lightroom export folders with XMP sidecar preservation. |
| `photosage undo` | Restore original filenames from a rename manifest. |
| `photosage ollama` | Inspect local Ollama vision model support. |
| `photosage manifest` | Inspect and validate PhotoSage manifests. |
| `photosage geocode` | Manage local reverse geocoding aliases and cache entries. |
| `photosage config` | Validate and inspect PhotoSage configuration. |
| `photosage benchmark` | Benchmark configured vision providers. |
| `photosage search` | Build and query a private local media search index. |

## `photosage doctor`

Check configuration, providers, paths, and optional media tools.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage doctor --help
```

## `photosage recover`

Inspect, resume, or roll back an interrupted manifest.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--manifest` | yes | `none` |  |
| `--resume` | no | `false` | Resume operations that are still safe to apply. |
| `--rollback` | no | `false` | Roll back completed operations. |
| `--output-json` | no | `none` | Write the recovery report as JSON. |

```powershell
photosage recover --help
```

## `photosage review`

Review, approve, reject, or edit proposed manifest filenames.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--manifest` | yes | `none` |  |
| `--approve` | no | `none` | Approve a filename or original path. Repeatable. |
| `--reject` | no | `none` | Reject a filename or original path. Repeatable. |
| `--rename` | no | `none` | Edit a proposal as ORIGINAL=NEW_FILENAME. Repeatable. |
| `--reviewer` | no | `local-user` | Reviewer label recorded in the manifest. |

```powershell
photosage review --help
```

## `photosage providers`

Show configured provider availability and local/cloud status.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--verbose` | no | `false` | Enable detailed console logging. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage providers --help
```

## `photosage duplicates`

Find likely duplicate photos and optionally export duplicate groups as JSON.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | yes | `none` | Photo directory to scan. |
| `--recursive`, `--no-recursive` | no | `true` | Scan nested folders. |
| `--output-json` | no | `none` | Write duplicate groups to JSON. |
| `--output-csv` | no | `none` | Write review recommendations to CSV. |
| `--review-folder` | no | `none` | Build a reviewed-manifest plan to move non-keepers into this folder. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage duplicates --help
```

## `photosage organize-preview`

Compare folder organization policies without creating folders or moving files.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | yes | `none` |  |
| `--policy` | no | `none` | Policy to preview. Repeatable. |
| `--output-json` | no | `none` |  |
| `--config` | no | `config/settings.yaml` |  |

```powershell
photosage organize-preview --help
```

## `photosage browse`

Generate private offline timeline and GPS map views.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | yes | `none` |  |
| `--output-html` | no | `photosage-browser.html` |  |
| `--output-json` | no | `none` |  |
| `--recursive`, `--no-recursive` | no | `true` |  |

```powershell
photosage browse --help
```

## `photosage watch`

Process stable files from a watched folder into an approval queue or apply after explicit approval.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | no | `none` | Folder to check for incoming photos. |
| `--manifest` | no | `none` | Reviewed watch queue manifest to apply. |
| `--apply` | no | `false` | Apply a reviewed watch queue manifest. Omit this to build an approval queue only. |
| `--recursive`, `--no-recursive` | no | `true` | Scan nested folders. |
| `--force-ai` | no | `false` | Mark files as AI-required. Watch mode does not call AI during queue creation. |
| `--provider` | no | `none` | Override configured provider. |
| `--local-only` | no | `false` | Prevent cloud provider usage. |
| `--verbose` | no | `false` | Enable detailed console logging. |
| `--output-json` | no | `none` | Write watch result to JSON. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage watch --help
```

## `photosage astro`

Preview or apply astrophotography filenames with capture-night grouping and FITS metadata.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | no | `none` | Astrophotography image directory. |
| `--manifest` | no | `none` | Reviewed astro preview manifest to apply. |
| `--apply` | no | `false` | Actually rename files. Omit this for preview only. |
| `--allow-unreviewed` | no | `false` | Build and apply an unreviewed astro plan. |
| `--profile` | no | `deep-sky` | Astro profile: lunar, solar, planetary, or deep-sky. |
| `--recursive`, `--no-recursive` | no | `true` | Scan nested folders. |
| `--force-ai` | no | `false` | Use configured AI provider if metadata is weak. |
| `--provider` | no | `none` | Override configured provider. |
| `--local-only` | no | `false` | Prevent cloud provider usage. |
| `--verbose` | no | `false` | Enable detailed console logging. |
| `--output-json` | no | `none` | Write astro manifest to JSON. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage astro --help
```

## `photosage scan`

Scan supported image files, score metadata, and show whether AI would be required.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | yes | `none` | Photo directory to scan. |
| `--recursive`, `--no-recursive` | no | `true` | Scan nested folders. |
| `--provider` | no | `none` | Override configured provider. |
| `--force-ai` | no | `false` | Treat every supported image as requiring AI. |
| `--local-only` | no | `false` | Prevent cloud provider usage. |
| `--verbose` | no | `false` | Enable detailed console logging. |
| `--output-json` | no | `none` | Write scan results to a JSON file. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage scan --help
```

## `photosage preview`

Preview proposed filename changes without renaming files.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | yes | `none` | Photo directory to preview. |
| `--recursive`, `--no-recursive` | no | `true` | Scan nested folders. |
| `--provider` | no | `none` | Override configured provider. |
| `--force-ai` | no | `false` | Show every file as AI-required when no precomputed AI JSON is available. |
| `--local-only` | no | `false` | Prevent cloud provider usage. |
| `--verbose` | no | `false` | Enable detailed console logging. |
| `--output-json` | no | `none` | Write preview manifest to a JSON file. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage preview --help
```

## `photosage rename`

Apply safe renames only when --apply is explicitly provided.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | no | `none` | Photo directory to rename. |
| `--manifest` | no | `none` | Reviewed preview manifest to apply. |
| `--apply` | no | `false` | Actually rename files. Required for any file changes. |
| `--allow-unreviewed` | no | `false` | Build and apply a new plan without reviewing a saved preview manifest. |
| `--recursive`, `--no-recursive` | no | `true` | Scan nested folders. |
| `--provider` | no | `none` | Override configured provider. |
| `--force-ai` | no | `false` | Mark files as AI-required when no precomputed AI JSON is available. |
| `--local-only` | no | `false` | Prevent cloud provider usage. |
| `--verbose` | no | `false` | Enable detailed console logging. |
| `--output-json` | no | `none` | Write rename manifest to a JSON file. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage rename --help
```

## `photosage lightroom-process`

Process Lightroom export folders with XMP sidecar preservation.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | no | `none` | Lightroom export directory to process. |
| `--manifest` | no | `none` | Reviewed Lightroom preview manifest to apply. |
| `--preview` | no | `false` | Preview Lightroom rename and organization operations. |
| `--apply` | no | `false` | Actually rename exported files and matching XMP sidecars. |
| `--allow-unreviewed` | no | `false` | Build and apply an unreviewed Lightroom plan. |
| `--organize` | no | `false` | Organize output into year/month/category folders. |
| `--preset` | no | `none` | Lightroom preset such as travel, astronomy, construction, metadata-only, or ai-heavy. |
| `--recursive`, `--no-recursive` | no | `true` | Scan nested folders. |
| `--provider` | no | `none` | Override configured provider. |
| `--force-ai` | no | `false` | Mark files as AI-required even when metadata is sufficient. |
| `--local-only` | no | `false` | Prevent cloud provider usage. |
| `--force-catalog-modify` | no | `false` | Allow processing inside probable Lightroom catalog paths. Not recommended. |
| `--verbose` | no | `false` | Enable detailed console logging. |
| `--output-json` | no | `none` | Write Lightroom manifest to a JSON file. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage lightroom-process --help
```

## `photosage undo`

Restore original filenames from a rename manifest.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--manifest` | yes | `none` | Rename manifest to roll back. |
| `--dry-run` | no | `false` | Validate and report rollback operations without moving files. |
| `--force` | no | `false` | Skip confirmation for real undo operations. |
| `--verbose` | no | `false` | Print each rollback operation and enable detailed logging. |
| `--continue-on-error`, `--stop-on-error` | no | `true` | Continue processing after failed operations. |
| `--recursive`, `--no-recursive` | no | `true` | Accepted for command consistency. Undo uses manifest paths. |
| `--provider` | no | `none` | Accepted for command consistency. Undo does not use providers. |
| `--force-ai` | no | `false` | Accepted for command consistency. Undo does not use AI. |
| `--local-only` | no | `false` | Accepted for command consistency. Undo does not use providers. |
| `--output-json` | no | `none` | Write rollback summary to a JSON file. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage undo --help
```

## `photosage ollama`

Inspect local Ollama vision model support.

### Subcommands

| Command | Purpose |
| --- | --- |
| `photosage ollama models` | List locally installed Ollama models. |
| `photosage ollama info` | Show best-effort Ollama diagnostics. |

```powershell
photosage ollama --help
```

## `photosage ollama models`

List locally installed Ollama models.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--endpoint` | no | `none` | Ollama endpoint override. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage ollama models --help
```

## `photosage ollama info`

Show best-effort Ollama diagnostics.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--endpoint` | no | `none` | Ollama endpoint override. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage ollama info --help
```

## `photosage manifest`

Inspect and validate PhotoSage manifests.

### Subcommands

| Command | Purpose |
| --- | --- |
| `photosage manifest validate` | Validate a manifest without moving files. |

```powershell
photosage manifest --help
```

## `photosage manifest validate`

Validate a manifest without moving files.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--manifest` | yes | `none` | Manifest file to validate. |
| `--hashes` | no | `false` | Compute SHA256 hashes for existing referenced files. |
| `--output-json` | no | `none` | Write validation report to JSON. |
| `--verbose` | no | `false` | Print validation issues. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage manifest validate --help
```

## `photosage geocode`

Manage local reverse geocoding aliases and cache entries.

### Subcommands

| Command | Purpose |
| --- | --- |
| `photosage geocode set` | Save a local GPS to location alias for filenames. |
| `photosage geocode list` | List cached GPS location names. |

```powershell
photosage geocode --help
```

## `photosage geocode set`

Save a local GPS to location alias for filenames.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--lat` | yes | `none` | Latitude. |
| `--lon` | yes | `none` | Longitude. |
| `--location` | yes | `none` | Location text to use in filenames. |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage geocode set --help
```

## `photosage geocode list`

List cached GPS location names.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--config` | no | `config/settings.yaml` | Alternate config file. |

```powershell
photosage geocode list --help
```

## `photosage config`

Validate and inspect PhotoSage configuration.

### Subcommands

| Command | Purpose |
| --- | --- |
| `photosage config validate` | Validate configuration types, ranges, providers, and filename tokens. |

```powershell
photosage config --help
```

## `photosage config validate`

Validate configuration types, ranges, providers, and filename tokens.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--config` | no | `config/settings.yaml` | Configuration file to validate. |

```powershell
photosage config validate --help
```

## `photosage benchmark`

Benchmark configured vision providers.

### Subcommands

| Command | Purpose |
| --- | --- |
| `photosage benchmark providers` | Compare provider latency, retries, validity, confidence, and routing. |

```powershell
photosage benchmark --help
```

## `photosage benchmark providers`

Compare provider latency, retries, validity, confidence, and routing.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | yes | `none` |  |
| `--provider` | no | `none` | Provider to benchmark. Repeatable. |
| `--limit` | no | `10` | Maximum number of sample images. |
| `--allow-cloud` | no | `false` | Permit billable cloud provider calls. |
| `--output-json` | no | `none` |  |
| `--output-markdown` | no | `none` |  |
| `--config` | no | `config/settings.yaml` |  |

```powershell
photosage benchmark providers --help
```

## `photosage search`

Build and query a private local media search index.

### Subcommands

| Command | Purpose |
| --- | --- |
| `photosage search index` | Index photo and video metadata into a local SQLite database. |
| `photosage search query` | Search the private local metadata and embedding index. |

```powershell
photosage search --help
```

## `photosage search index`

Index photo and video metadata into a local SQLite database.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `--input` | yes | `none` |  |
| `--recursive`, `--no-recursive` | no | `true` |  |
| `--config` | no | `config/settings.yaml` |  |

```powershell
photosage search index --help
```

## `photosage search query`

Search the private local metadata and embedding index.

### Options

| Option | Required | Default | Description |
| --- | --- | --- | --- |
| `query` | yes | `none` | Natural-language search text. |
| `--limit` | no | `20` |  |
| `--output-json` | no | `none` |  |
| `--config` | no | `config/settings.yaml` |  |

```powershell
photosage search query --help
```
